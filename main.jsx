import React from "react";
import ReactDOM from "react-dom/client";
import ExsculptumLandingPage from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ExsculptumLandingPage />
  </React.StrictMode>
);
